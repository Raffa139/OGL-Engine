package de.ren.ogl.engine.controller.mouse;

public class MouseMiddleDownEvent extends MouseButtonDownEvent {
  public MouseMiddleDownEvent(Object source, long window, int button, int mods) {
    super(source, window, button, mods);
  }
}
